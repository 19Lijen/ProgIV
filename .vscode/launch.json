{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Taller UTP",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/Parcial_2"
            }
        }
    ]
}